import styled from 'styled-components'
import { tm } from './theme'
export default styled.h1`
  color: white;
  padding: ${tm`spacing.small`};
  background-image: ${tm`gradient.secondary`};
  box-shadow: ${tm`shadow.small`};
`
