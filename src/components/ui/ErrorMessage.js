import styled from 'styled-components'
import { tm } from './theme'
export default styled.span`
  text-align: center;
  padding: ${tm`spacing.small`};
  border: 3px solid ${tm`color.warning`};
  color: ${tm`color.warning`};
  margin: ${tm`spacing.medium`};
`
